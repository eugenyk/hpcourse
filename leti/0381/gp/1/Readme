Build Requirements

1. Java 1.8
2. Apache Maven (version >= 3.2.5)

Build command

	mvn clean package

Recommendations

1. The package is located in the "target" folder
2. "lib" folder must be in the same directory as the package
3. Server configuration file "config.properties" must be in the same directory as the package, otherwise the server will start with the default settings