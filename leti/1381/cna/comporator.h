#ifndef COMPORATOR_H
#define COMPORATOR_H

/*template <typename T>
class MaxComporator
{
public:
    MaxComporator(T min): m_value(min) { }

    bool operator(T value) {
        if(value > m_value)
    }

private:
    T m_value;
};*/

/*template <typename T>
class MinComporator
{
public:
    MinComporator();
};

template <typename T>
class EqualComporator
{
public:
    EqualComporator(T value): m_value(value) { }

    bool operator(T value);// { return value == m_value; }

private:
    T m_value;
};*/

#endif // COMPORATOR_H
